apiVersion: v1
kind: Service
metadata:
  name: registry
  labels:
    app: registry
spec:
  ports:
  - name: http
    port: 8761
  clusterIP: None
  selector:
    app: registry

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: registry
spec:
  serviceName: registry
  replicas: 2
  selector:
    matchLabels:
      app: registry
  template:
    metadata:
      labels:
        app: registry
    spec:
      containers:
      - name: registry
        image: shuaicj/cloud-registry:1.1.0
        env:
        - name: PORT
          value: "8761"
        - name: REGISTRY_SERVICE_URL
          value: "http://registry-0.registry.default.svc.cluster.local:8761/eureka,http://registry-1.registry.default.svc.cluster.local:8761/eureka"
        - name: REGISTRY_SERVICE_HA
          value: "true"
        ports:
        - name: http
          containerPort: 8761

