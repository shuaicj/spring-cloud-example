spring:
  application:
    name: service-b
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config

eureka:
  client:
    serviceUrl:
      defaultZone: ${REGISTRY_SERVICE_URL:http://localhost:8761/eureka}
