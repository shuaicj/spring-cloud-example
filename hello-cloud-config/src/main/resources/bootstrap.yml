server:
  port: ${PORT:8888}

spring:
  application:
    name: config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config

management:
  security:
    enabled: false
